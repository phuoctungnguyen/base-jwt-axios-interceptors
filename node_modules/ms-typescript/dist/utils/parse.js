"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parse = void 0;
var constants_1 = require("./constants");
var parse = function (str) {
    if (str.length > 100) {
        return 0;
    }
    var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(str);
    if (!match) {
        return 0;
    }
    var n = parseFloat(match[1]);
    var type = (match[2] || 'ms').toLowerCase();
    if (constants_1.YEAR_TYPES.has(type)) {
        return n * constants_1.Y;
    }
    if (constants_1.WEEK_TYPES.has(type)) {
        return n * constants_1.W;
    }
    if (constants_1.DAY_TYPES.has(type)) {
        return n * constants_1.D;
    }
    if (constants_1.HOUR_TYPES.has(type)) {
        return n * constants_1.H;
    }
    if (constants_1.MINUTE_TYPES.has(type)) {
        return n * constants_1.M;
    }
    if (constants_1.SECONDS_TYPES.has(type)) {
        return n * constants_1.S;
    }
    if (constants_1.MS_TYPES.has(type)) {
        return n;
    }
    return 0;
};
exports.parse = parse;
